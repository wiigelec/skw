# builder.toml

[paths]
# Workspace for cloning and building book sources
build_dir = "build"

# Directory for storing final packages (future use)
package_dir = "packages"

# Directory containing all book and profile configurations
profiles_dir = "profiles"
